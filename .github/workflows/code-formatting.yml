name: Check C++ code formatting

on:
  push:
    branches:
      - '**' # Trigger on push to any branch
  pull_request:
    branches:
      - main # Trigger on pull requests targeting the main branch

jobs:
  format-check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install clang-format
      run: |
        sudo apt-get update
        sudo apt-get install clang-format -y
        clang-format --version
    - name: Run clang-format to check for unformatted code
      id: clang_format_check
      run: |
        find . -iname '*.hpp' -o -iname '*.cpp' | xargs clang-format --dry-run --Werror --style=Google
      continue-on-error: true  # Allow other steps to run even if this fails

  warn-unformatted:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref != 'refs/heads/main'
    needs: format-check
    steps:
    - name: Warn user about unformatted code
      if: steps.clang_format_check.outcome == 'failure'
      run: |
        echo "Warning: Your code is not formatted according to clang-format. Please format the code."

  block-unformatted:
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    needs: format-check
    steps:
    - name: Fail PR on unformatted code
      if: steps.clang_format_check.outcome == 'failure'
      run: |
        echo "Error: Your code is not formatted according to clang-format. The pull request cannot be merged into the main branch."
        exit 1  # Explicitly fail the PR if the code is unformatted
